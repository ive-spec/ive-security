<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>ar.ive.spec</groupId>
	<artifactId>ive-security</artifactId>
	<version>1.0.0</version>
	<name/>
	<description>The IVE credential and permission guard: the catalog's security requirements and their mechanics, without a web framework.</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!-- Same JUnit and surefire as ive-errors-runtime, pinned through the
		     parent's own properties (see that pom). -->
		<junit-jupiter.version>5.10.2</junit-jupiter.version>
		<maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
	</properties>
	<!--
		NO WEB FRAMEWORK, ON PURPOSE. The guard is plain Java: the Spring
		interceptor (or any other adapter) stays generated in each project
		and delegates here. The parent is inherited only for version
		management and the compiler configuration.
	-->
	<dependencies>
		<!-- The error families, the causes (Conditions), the guide
		     (Guidances) and the credential port (ar.ive.spec.security).
		     Coordinates from iveRuntimeDependency.js, like every consumer. -->
		<dependency>
			<groupId>com.github.ive-spec</groupId>
			<artifactId>ive-errors-runtime</artifactId>
			<version>1.0.4</version>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<repositories>
		<repository>
			<id>jitpack.io</id>
			<url>https://jitpack.io</url>
		</repository>
	</repositories>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
			</plugin>
		</plugins>
	</build>

</project>
